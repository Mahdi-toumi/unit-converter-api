apiVersion: v1
kind: ConfigMap
metadata:
  name: unit-converter-config
  labels:
    app: unit-converter
data:
  # API Configuration
  API_TITLE: "Unit Converter API"
  API_VERSION: "1.0.0"
  API_DESCRIPTION: "REST API for unit conversions"

  # Server Configuration
  HOST: "0.0.0.0"
  PORT: "8000"

  # Logging
  LOG_LEVEL: "INFO"

  # External API
  CURRENCY_API_URL: "https://api.exchangerate-api.com/v4/latest"
  CURRENCY_API_TIMEOUT: "5"
